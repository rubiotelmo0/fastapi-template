# Template Docs

This folder contains starter documentation assets for the template:

- `bruno_collection/`: example Bruno requests for the sample `items` endpoints.
- `uml/architecture.puml`: a small PlantUML diagram showing how the layers fit together.

When you replace the sample `Item` entity with your own domain, update these files so the docs stay aligned with the code.
