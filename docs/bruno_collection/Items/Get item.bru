meta {
  name: Get item
  type: http
  seq: 3
}

get {
  url: {{baseURL}}/items/{{itemId}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.id: isNumber
  res.body.name: isNotEmpty
}

settings {
  encodeUrl: true
}
