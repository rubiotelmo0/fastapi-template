meta {
  name: List items
  type: http
  seq: 2
}

get {
  url: {{baseURL}}/items
  body: none
  auth: inherit
}

vars:post-response {
  itemId: res('[0].id')
}

assert {
  res.status: eq 200
  res.body: isArray
}

settings {
  encodeUrl: true
}
