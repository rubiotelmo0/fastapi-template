meta {
  name: Delete item
  type: http
  seq: 6
}

delete {
  url: {{baseURL}}/items/{{itemId}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.detail: isNotEmpty
}

settings {
  encodeUrl: true
}
